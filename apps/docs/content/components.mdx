---
title: Component Map
description: Quick reference for all available components, their CLI commands, and categories.
---

# Component Map

## High-Level Components

| Name | Command | Description |
|---|---|---|
| `vform` | `buildpad add vform` | Dynamic form — renders 40+ interface types |
| `vtable` | `buildpad add vtable` | Dynamic table with sorting, selection, drag-drop |
| `collection-form` | `buildpad add collection-form` | CRUD wrapper with data fetching (uses VForm) |
| `collection-list` | `buildpad add collection-list` | Dynamic table with pagination |

## Input Components

| Name | Command |
|---|---|
| `input` | `buildpad add input` |
| `textarea` | `buildpad add textarea` |
| `input-code` | `buildpad add input-code` |
| `input-block-editor` | `buildpad add input-block-editor` |
| `tags` | `buildpad add tags` |
| `slider` | `buildpad add slider` |

## Selection Components

| Name | Command |
|---|---|
| `select-dropdown` | `buildpad add select-dropdown` |
| `select-radio` | `buildpad add select-radio` |
| `select-multiple-checkbox` | `buildpad add select-multiple-checkbox` |
| `select-multiple-checkbox-tree` | `buildpad add select-multiple-checkbox-tree` |
| `select-multiple-dropdown` | `buildpad add select-multiple-dropdown` |
| `select-icon` | `buildpad add select-icon` |
| `autocomplete-api` | `buildpad add autocomplete-api` |
| `collection-item-dropdown` | `buildpad add collection-item-dropdown` |
| `color` | `buildpad add color` |

## Boolean Components

| Name | Command |
|---|---|
| `boolean` | `buildpad add boolean` |
| `toggle` | `buildpad add toggle` |

## DateTime

| Name | Command |
|---|---|
| `datetime` | `buildpad add datetime` |

## Media Components

| Name | Command |
|---|---|
| `file` | `buildpad add file` |
| `file-image` | `buildpad add file-image` |
| `files` | `buildpad add files` |
| `upload` | `buildpad add upload` |

## Relational Components

| Name | Command |
|---|---|
| `list-m2m` | `buildpad add list-m2m` |
| `select-dropdown-m2o` | `buildpad add select-dropdown-m2o` |
| `list-o2m` | `buildpad add list-o2m` |
| `list-m2a` | `buildpad add list-m2a` |

## Layout Components

| Name | Command |
|---|---|
| `divider` | `buildpad add divider` |
| `notice` | `buildpad add notice` |
| `group-detail` | `buildpad add group-detail` |

## Rich Text Components

| Name | Command |
|---|---|
| `rich-text-html` | `buildpad add rich-text-html` |
| `rich-text-markdown` | `buildpad add rich-text-markdown` |

## Map Components

| Name | Command |
|---|---|
| `map` | `buildpad add map` |
| `map-with-real-map` | `buildpad add map-with-real-map` |

## Workflow Components

| Name | Command |
|---|---|
| `workflow-button` | `buildpad add workflow-button` |
