/**
 * `users` namespace — ui-users (UsersManager, UserDetail, RolesManager, RoleDetail, PoliciesManager, PolicyDetail, ModuleAccessKeysManager, …).
 *
 * Filled in by the users migration (docs/I18N_COMPONENT_GUIDE.md). Keep the
 * defaults and the Indonesian catalog in step — the parity test enforces it.
 */
export interface UsersTranslations {}

export const usersDefaults: UsersTranslations = {};

export const usersId: UsersTranslations = {};
