/**
 * `interfaces.textarea` — the Textarea interface renders only caller-supplied
 * text (label, placeholder, description, error) and has no strings of its own.
 */
export interface TextareaTranslations {}

export const textareaDefaults: TextareaTranslations = {};

export const textareaId: TextareaTranslations = {};
