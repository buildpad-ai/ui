/** `interfaces.tags` — strings of the Tags interface (filled in by the ui-interfaces migration). */
export interface TagsTranslations {}

export const tagsDefaults: TagsTranslations = {};

export const tagsId: TagsTranslations = {};
