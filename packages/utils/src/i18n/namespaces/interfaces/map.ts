/** `interfaces.map` — strings of the Map interface (filled in by the ui-interfaces migration). */
export interface MapTranslations {}

export const mapDefaults: MapTranslations = {};

export const mapId: MapTranslations = {};
