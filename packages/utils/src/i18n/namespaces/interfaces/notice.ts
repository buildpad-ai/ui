/**
 * `interfaces.notice` — the Notice interface renders only caller-supplied
 * content (title, text, children) and has no strings of its own.
 */
export interface NoticeTranslations {}

export const noticeDefaults: NoticeTranslations = {};

export const noticeId: NoticeTranslations = {};
