/** `interfaces.file` — strings of the File interface (filled in by the ui-interfaces migration). */
export interface FileTranslations {}

export const fileDefaults: FileTranslations = {};

export const fileId: FileTranslations = {};
