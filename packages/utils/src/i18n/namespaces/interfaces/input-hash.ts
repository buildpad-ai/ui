/** `interfaces.inputHash` — strings of the InputHash interface. */
export interface InputHashTranslations {
  /** Placeholder shown while a hashed value is stored and nothing new is typed */
  storedPlaceholder: string;
}

export const inputHashDefaults: InputHashTranslations = {
  storedPlaceholder: 'Value securely stored',
};

export const inputHashId: InputHashTranslations = {
  storedPlaceholder: 'Nilai tersimpan dengan aman',
};
