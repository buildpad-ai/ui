diff --git a/add-i18n/SKILL.md b/add-i18n/SKILL.md
index 5552158..50fd62f 100644
--- a/add-i18n/SKILL.md
+++ b/add-i18n/SKILL.md
@@ -19,7 +19,7 @@ The `[lang]` route segment is the single source of truth for both. Middleware ne
 
 ## CRITICAL Rules
 
-1. **Never edit strings inside `components/ui/*`.** Those files are Copy & Own; every edited file diverges its checksum and turns the next `buildpad upgrade` into a manual merge. Pass the overrides the components already accept (`translations` on `ListM2M`, `loadingText`/`noItemsText` on `VTable`, `locale` on `VForm`) and record the remaining English chrome as a known gap (see [Known Limits](#known-limits)).
+1. **Never edit strings inside `components/ui/*`.** Those files are Copy & Own; every edited file diverges its checksum and turns the next `buildpad upgrade` into a manual merge. Buildpad components read their strings from `BuildpadI18nProvider` (mounted by `lib/i18n/provider.tsx`): put translations under the `buildpad` namespace of `lib/i18n/dictionaries/<code>.json` — its shape is `BuildpadTranslations` from `lib/buildpad/i18n/types.ts` (`common`, `form`, `table`, `collections`, `interfaces.<name>`, `files`, `users`, `forms`, `hooks`), and Buildpad ships a complete `id` catalog (`lib/buildpad/i18n/locales/id.ts`) that the provider applies automatically. Per-instance `translations` props (every main component has one) still win over the dictionary.
 2. **Never put content in dictionaries or UI strings in DaaS.** Dictionaries ship with the app; content lives in translation rows. Mixing them breaks both editing and deployment.
 3. **One locale list.** `lib/i18n/config.ts` is the only place locale codes are declared. The `languages.code` values in DaaS **must match it exactly** (`id` ≠ `id-ID`) or content queries silently return nothing.
 4. **Never use `special: ["translations"]` on DaaS today.** The read path understands it, but the item writer has no nested-write branch for it, so translation rows submitted through the parent form are silently dropped. Use `special: ["o2m"]` — [content-translations reference](references/content-translations.instructions.md) explains the recipe and why.
@@ -37,7 +37,7 @@ ls -d app/\[lang\] 2>/dev/null && echo "ALREADY LOCALE-PREFIXED"
 
 - Requires Node.js v24 LTS and pnpm v10+ (see [add-buildpad](../add-buildpad/SKILL.md)).
 - The app must be CLI-scaffolded (`buildpad.json` present) with the Supabase middleware installed (`lib/supabase/middleware.ts`). Next.js 16 apps may name the root middleware `proxy.ts` instead of `middleware.ts` — the same changes apply to whichever file exists.
-- **Decide the shape first.** If `app/[lang]/` already exists (apps generated by a CLI that ships locale routing by default), skip Step 2 and Step 3's restructure and only add locales, dictionaries, and content translations. Otherwise this is a **retrofit**: it edits CLI-owned files (`middleware.ts`, `lib/supabase/middleware.ts`, `app/layout.tsx`, every page under `app/`), each of which will show as modified on `buildpad status`. Tell the user this before starting.
+- **Decide the shape first.** Apps scaffolded by `@buildpad/cli` ≥ 2.3 are locale-prefixed out of the box: `app/[lang]/` exists, `lib/i18n/*` and `components/LanguageSwitcher.tsx` are installed as the tracked `i18n` lib module, and the middleware, login page, app shell and route pages are already locale-aware. For those apps **skip Steps 1–3, 5 and 6** — only add locales (`buildpad init --locales` is equivalent to editing the marker blocks in `lib/i18n/config.ts`), translate `lib/i18n/dictionaries/<code>.json`, and do Step 7. An app scaffolded earlier is a **retrofit**: run `npx @buildpad/cli@latest migrate i18n --cwd . [--locales en,id]` then `upgrade --all --three-way` instead of hand-editing CLI-owned files; only the user's own pages then need `useLocaleRouter()` / `localeHref` (the command prints the grep). Never patch `middleware.ts`, `lib/supabase/middleware.ts`, `app/layout.tsx` or `components/layout/AuthenticatedShell.tsx` by hand on a CLI-managed app — `upgrade` ships the locale-aware versions.
 - DaaS MCP tools (`schema`, `collections`, `fields`, `items`, `permissions`) must be reachable for the content layer — see [daas-platform](../daas-platform/SKILL.md).
 - DaaS CORS must use explicit origins (the LanguageSwitcher persists the choice with a credentialed call).
 
@@ -60,7 +60,7 @@ What does the user need?
 
 ## Step 1 — Locale config
 
-Create `lib/i18n/config.ts` from the [locale-routing reference](references/locale-routing.instructions.md#1-locale-config). It exports `locales`, `defaultLocale`, `localeMeta` (display name + `ltr`/`rtl`), `LOCALE_COOKIE`, and the helpers `hasLocale`, `getLocaleFromPathname`, `stripLocale`, `localeHref`. Everything else imports from here.
+`lib/i18n/config.ts` is installed by the CLI (`i18n` lib module; `buildpad add i18n` on older apps). It exports `locales`, `defaultLocale`, `localeMeta` (display name + `ltr`/`rtl`), `LOCALE_COOKIE`, and the helpers `hasLocale`, `getLocaleFromPathname`, `stripLocale`, `localeHref`. Add locales with `npx @buildpad/cli@latest init --locales en,id` (or `migrate i18n --locales …`), which rewrites the `// buildpad:locales`, `// buildpad:locale-meta-start/end` and `// buildpad:dictionary-loaders-start/end` blocks and seeds `dictionaries/<code>.json` from `en.json`. The reference file shows the shape for apps that are not CLI-managed.
 
 ## Step 2 — Middleware
 
@@ -95,8 +95,8 @@ The Next.js guide keeps dictionaries in Server Components. Buildpad pages are cl
 
 Dictionary shape — two namespaces:
 
-- `app.*` — your pages (`app.login.submit`, `app.nav.content`, …).
-- `buildpad.*` — what the Buildpad components can accept today: `buildpad.listM2M` is passed straight into `ListM2M`'s `translations` prop (same `{placeholder}` interpolation convention), `buildpad.table.loading` / `buildpad.table.noItems` into `VTable`.
+- `app.*` — your pages (`app.login.submit`, `app.nav.content`, …). The CLI's `en.json` already carries the keys the scaffolded login page, app shell (`app.shell.*`, `app.nav.*`), content, scope and forms pages use.
+- `buildpad.*` — overrides for Buildpad components, shaped exactly like `BuildpadTranslations` (`lib/buildpad/i18n/types.ts`): `buildpad.interfaces.listM2M.create_new`, `buildpad.table.noItems`, `buildpad.form.validation.required`, `buildpad.common.itemCount.other`, … `I18nProvider` merges it over Buildpad's own catalog for the locale (`en` and `id` ship with the components), so for `id` the namespace can stay `{}`; for other locales translate the keys you see in `lib/buildpad/i18n/namespaces/*.ts`.
 
 Every locale file must have the same keys; `types.ts` derives `Dictionary` from `en.json`, so a missing key in `id.json` is a type error in `dictionaries.ts`.
 
@@ -116,7 +116,7 @@ Replace every hardcoded route literal with `useLocaleRouter()` (drop-in for `use
 
 ## Step 6 — LanguageSwitcher
 
-`components/LanguageSwitcher.tsx` is a `SelectDropdown` (`choices` from `localeMeta`, `allowNone={false}`) whose `onChange` calls `useSwitchLocale()`. That hook writes the `NEXT_LOCALE` cookie the middleware reads and pushes the same route under the new prefix. Optionally persist the choice to the user's DaaS profile: DaaS lets a user `PATCH /api/users/me` with `language` (the column exists, default `en-US`), so mirror the direct-DaaS call pattern from `lib/buildpad/hooks/useUsers.ts`. Mount the switcher in the `AuthenticatedShell` header (pass it through a `'use client'` wrapper, as the shell's docs describe for `navItems`) and on the login page.
+`components/LanguageSwitcher.tsx` is installed by the `i18n` module and already rendered by `AuthenticatedShell`'s header (`showLanguageSwitcher={false}` hides it; `headerActions` adds your own controls) and by the login page. It writes the `NEXT_LOCALE` cookie, re-enters the current route under the new prefix (`useSwitchLocale()`), and best-effort persists `language` to the DaaS profile through `/api/auth/user`. It renders nothing while only one locale is configured. Nothing to build here on a CLI-managed app.
 
 ## Step 7 — Content translations (optional)
 
@@ -170,8 +170,8 @@ Matches in checks 1–3 must be fixed before the work is complete (hits inside `
 
 ## Known Limits
 
-- **Component chrome stays English until Buildpad UI ships an i18n provider.** `CollectionList`, `CollectionForm`, `FileManager`, `UsersManager`, `Upload`, and most interfaces have hardcoded strings with no override props. Do not patch them; log the gap. (Tracked as Phase 2 of the Buildpad UI i18n plan.)
-- **Dates and numbers inside `components/ui/*`** use the browser locale, not the URL locale. Use `formatDate`/`formatNumber` from `useI18n()` in your own code.
+- **Component chrome follows the dictionary** for every Buildpad package since `@buildpad/cli` 2.3 (`ui-form`, `ui-table`, `ui-collections`, `ui-interfaces`, `ui-files`, `ui-users`, `ui-forms`, relation hooks). Copies installed from an earlier release still render English chrome: run `buildpad upgrade --all --three-way` rather than patching them. Third-party UI inside components (MapLibre controls, EditorJS internals beyond the wired toolbox) keeps its own language.
+- **Dates and numbers inside components** use the provider's locale and a pinned time zone (UTC unless the app passes `timeZone` to `I18nProvider`). Use `formatDate`/`formatNumber` from `useI18n()` in your own code.
 - **RTL**: setting `dir` on `<html>` and Mantine's `DirectionProvider` is the extent of support; component CSS is not RTL-audited. Do not add an RTL locale to `languages` unless the user accepts this.
 - **Not in scope**: translating the DaaS admin app itself, form-builder definitions authored at runtime, email templates (DaaS mail is a raw transport — callers own the copy), `hreflang`/localized sitemaps for the authenticated tree (only public pages such as `/[lang]/login` benefit; add `alternates.languages` there if the user wants it).
 - **DaaS gaps that a skill cannot fix**: no `deep` filter on nested reads, no `translations` interface or junction wizard in the admin. The `o2m` recipe is the supported path until DaaS implements the Directus `translations` field type end to end.
diff --git a/add-i18n/references/locale-routing.instructions.md b/add-i18n/references/locale-routing.instructions.md
index b38bef1..0d95ec2 100644
--- a/add-i18n/references/locale-routing.instructions.md
+++ b/add-i18n/references/locale-routing.instructions.md
@@ -8,6 +8,8 @@ applyTo: "**/*.{ts,tsx,json}"
 
 All code below assumes the CLI-scaffolded layout (`buildpad bootstrap`): Supabase middleware in `lib/supabase/middleware.ts`, `publicOrigin()` in `lib/origin.ts`, Buildpad components in `components/ui`, and `lib/buildpad/*`. Adjust import paths if the project uses `src/`.
 
+> **Source of truth.** Since `@buildpad/cli` 2.3 every file in §1–§7 is shipped by the CLI's `i18n` lib module and `[lang]` templates (`packages/cli/templates/lib/i18n/*`, `templates/components/LanguageSwitcher.tsx`, `templates/app/layout.tsx`, `templates/middleware/middleware.ts`, `templates/supabase/middleware.ts` in `buildpad-ai/ui`). On a CLI-managed app do not re-create them — `buildpad add i18n` / `migrate i18n` / `upgrade` do. The listings below are kept for apps that are not CLI-managed and as documentation of the contract (cookie name, redirect order, `localeHref` semantics); where they differ, the templates win. Notable differences in the shipped versions: `I18nProvider` also mounts `BuildpadI18nProvider` with the app's `buildpad.*` dictionary namespace; `useLocaleRouter()` also exposes `prefetch`/`refresh`; `NavItem` carries `labelKey`/`sectionKey` dictionary paths; the switcher is a Mantine `Select` that persists through `/api/auth/user`.
+
 ---
 
 ## 1. Locale config
